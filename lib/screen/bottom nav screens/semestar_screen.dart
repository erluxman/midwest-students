import 'package:flutter/material.dart';

class SemestarScreen extends StatelessWidget {
  const SemestarScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
